{
  "name": "limos-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/write-config.mjs",
    "check": "node --check app.js && node --check api/state.js && node --check scripts/write-config.mjs",
    "deploy": "npx vercel --prod",
    "serve": "python3 -m http.server 5173 --bind 127.0.0.1"
  }
}
